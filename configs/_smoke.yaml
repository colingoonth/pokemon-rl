# Throwaway smoke config for validating the launcher / clobber-guard / snapshot.
# Tiny + RND off so it finishes in ~1-2 min. Safe to delete.
run_name: smoke_launch
state_path: states/blue_fight.state
total_timesteps: 40000
n_envs: 8
max_steps: 4096
n_steps: 64
n_epochs: 2
minibatch_size: 64
learning_rate: 2.5e-4
gamma: 0.999
gae_lambda: 0.95
clip_coef: 0.1
value_coef: 0.5
entropy_coef: 0.01
max_grad_norm: 0.5
anneal_lr: true
seed: 0
log_every: 2
save_every: 0
async_envs: true
reward_class: RewardV0_5_skeleton
frame_skip: 1
torch_threads: 4
vec_context: spawn
rnd_enabled: false
