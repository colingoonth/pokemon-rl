# V0.2.6: inherits V0.2.4_f25 reward balance (FAINT -25, NEW_MAP +5, etc.)
# plus +10 Mart/PC first-visit bonus and +20 Pewter Gym first-visit bonus.
# entropy_coef 0.03 — between f25's collapse-prone 0.01 and V0.2.5's
# commit-prevention 0.1. Dialog detection deferred to V0.2.7 pending
# RAM-byte verification.
run_name: elsa_brock_v0_2_6_ddp4
total_timesteps: 200_000_000
n_envs: 32
n_steps: 256
n_epochs: 4
minibatch_size: 512
learning_rate: 2.5e-4
gamma: 0.999
gae_lambda: 0.95
clip_coef: 0.1
value_coef: 0.5
entropy_coef: 0.03
max_grad_norm: 0.5
anneal_lr: true
seed: 0
log_every: 10
async_envs: true
reward_class: RewardV0_2_6
