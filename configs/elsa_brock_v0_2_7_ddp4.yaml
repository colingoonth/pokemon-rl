# V0.2.7: V0.2.6 + bumped combat rewards.
# BEAT_MON 10 -> 20 (drops fight-EV breakeven from 74% to 60% win confidence)
# TRAINER_WIN_BONUS 20 -> 30 (trainer battles on Route 2 are the V1 critical path)
# Everything else inherited from V0.2.6 (FAINT -25, MART_PC +10, GYM +20,
# entropy_coef 0.03).
run_name: elsa_brock_v0_2_7_ddp4
total_timesteps: 200_000_000
n_envs: 32
n_steps: 256
n_epochs: 4
minibatch_size: 512
learning_rate: 2.5e-4
gamma: 0.999
gae_lambda: 0.95
clip_coef: 0.1
value_coef: 0.5
entropy_coef: 0.03
max_grad_norm: 0.5
anneal_lr: true
seed: 0
log_every: 10
async_envs: true
reward_class: RewardV0_2_7
