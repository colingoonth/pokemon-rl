# V0.3.2 h10: V0.3.1 + PC_HEAL_LOW bumped 5 -> 10 (conservative arm).
# Smallest measurable change. Tests whether 2x is enough for the agent
# to discover and learn the heal chain without distorting the rest of
# the reward landscape.
run_name: elsa_brock_v0_3_2_h10_ddp4
state_path: states/blue_fight.state
total_timesteps: 200_000_000
n_envs: 32
n_steps: 256
n_epochs: 4
minibatch_size: 512
learning_rate: 2.5e-4
gamma: 0.999
gae_lambda: 0.95
clip_coef: 0.1
value_coef: 0.5
entropy_coef: 0.03
max_grad_norm: 0.5
anneal_lr: true
seed: 0
log_every: 10
async_envs: true
reward_class: RewardV0_3_2_h10
